Security Rules

Never store plaintext passwords.

Never store GitHub tokens in SQLite.

Always use OS secure credential storage.

Never log secrets.

Never expose tokens to frontend.

Never print secrets in console.

Always validate filesystem paths.

Never execute arbitrary shell commands.

Escape every external input.

OAuth tokens must be revocable.

Support logout.

Support account removal.

Deleting an account must also delete secrets.