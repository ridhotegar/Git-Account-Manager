Backend Design

Rust is the source of truth.

Modules

accounts/

git/

ssh/

oauth/

credential/

repository/

database/

settings/

services/

utils/

Every module must be independent.

Business logic must never exist inside Tauri commands.

Tauri commands should only validate input and call services.

Every service must be unit testable.

Avoid global mutable state.

Prefer dependency injection.

Prefer traits over tightly coupled implementations.